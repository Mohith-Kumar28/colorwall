"use client";

import React from "react";

const Logo = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontSize: "28px",
        fontWeight: 700,
        color: "#1a1a1a",
        letterSpacing: "-0.5px",
      }}
    >
      🎨 Colorwall
    </div>
  );
};

export default Logo;
